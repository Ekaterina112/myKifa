import React from 'react';
import s from './Preview.module.css'

const PreviewBlock = () => {
    return (
        <div className={s.previewBlockContainer}>
            here will be preview information
        </div>
    );
};

export default PreviewBlock;